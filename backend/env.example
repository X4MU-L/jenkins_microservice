# Server settings
PORT=
NODE_ENV=

# MongoDB connection
MONGODB_URI=

# JWT settings
JWT_SECRET=strong_production_secret_keep_this_secure
JWT_EXPIRATION=1h

# MongoDB credentials (for docker-compose)
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=

# Logging
LOG_LEVEL=
